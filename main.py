from notebooks.scripts.extract import convert_yaml_to_csv

if __name__ == "__main__":
    """
    Main function to convert YAML files to CSV format.
    """
    print("Starting conversion of YAML files to CSV format...")
    convert_yaml_to_csv()
    print("Conversion completed successfully.")